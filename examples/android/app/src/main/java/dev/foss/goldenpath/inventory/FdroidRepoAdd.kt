package dev.foss.goldenpath.inventory

import android.content.Context

/** Indexes download in DevPulse. Do not open F-Droid or Droid-ify. */
object FdroidRepoAdd {
    fun open(context: Context, repoId: String) {
        if (repoId.isBlank()) return
    }
}
